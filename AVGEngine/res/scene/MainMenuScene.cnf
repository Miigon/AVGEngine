[Scene] =
{
	#创建并初始化控件容器
	[WidgetContainer] = 
	{
		#自定义的控件的基类都是WidgetContainer
		[res.widget.Dialog.cnf]dialog1 =
		{
			text = Test!
			size.width = $(size.width)
			size.height = $(size.height) * 0.2
			pos.x = -200
			pos.y = $(size.height) * 0.8
		}
	}
}